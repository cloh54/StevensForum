<main>

    <div class="logout-container">
        <a href="/logout">Logout</a>
    </div>
    <h1>{{user.username}}</h1>
    <h2>Total Reputation: {{reputation}}</h2>
    <div class="tab">
        <button class="tablinks" id="posts-made-button" onclick="showTab(event, 'posts-made-container')">Your posts</button>
        <button class="tablinks" id="posts-commented-in-button" onclick="showTab(event, 'posts-commented-in-container')">Posts you commented in</button>
    </div>

    <div class="tabcontent" id="posts-made-container">
        <h3>Your posts</h3>
        <ul>
            {{#each postsMade}}
            <li>
                <div class="post-li">
                    <a href="/posts/{{_id}}">{{topic}}</a>
                    <p>{{body}}</p>
                    <div class="post-details">
                        <p>
                            Tags:
                            {{#each tags}}
                               #{{this}}
                            {{/each}}
                        </p>
                    </div>
                </div>
            </li>
            {{/each}}
        </ul>
    </div>

    <div class="tabcontent" id="posts-commented-in-container">
        <h3>Posts you commented in</h3>
        <ul>
            {{#each postsCommentedIn}}
            <li>
                <div class="post-li">
                    <a href="/posts/{{_id}}">{{topic}}</a>
                    <p>{{body}}</p>
                    <div class="post-details">
                        <p>
                            Tags:
                            {{#each tags}}
                               #{{this}}
                            {{/each}}
                        </p>
                    </div>
                </div>
            </li>
            {{/each}}
        </ul>
    </div>
    
    
</main>